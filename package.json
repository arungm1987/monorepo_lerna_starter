{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --stream",
    "create_nx_workspace": "nx create-nx-workspace@latest",
    "run_test_header": "lerna run test --scope=header",
    "run_test_footer": "lerna run test --scope=footer",
    "nx_rest": "nx reset",
    "nx_graph": "nx graph"
  }
}
